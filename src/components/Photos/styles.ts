import { createStyles } from '@material-ui/core'

export const styles = () => createStyles({
  gridRoot: {
    width: '100%',
    display: 'flex'
  },
  gridItem: {
    padding: '5px'
  },
  pagination: {
    marginLeft: '100px'
  }
})
